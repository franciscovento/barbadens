/* eslint-disable no-unused-vars */
export enum Sleeve {
  'Manga larga',
  'Manga corta',
}

export enum Cuff {
  buttons,
  cuffLink,
}

export enum Pocket {
  without,
  with,
}

export enum Collar {
  italian,
  classicFrench,
  hiddenButtonDown,
  american,
  mao,
  cuban,
}
